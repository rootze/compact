% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AlertSystemScore.R
\name{.summarize_module_scores}
\alias{.summarize_module_scores}
\title{Summarize pathway scores across cells or groups}
\usage{
.summarize_module_scores(
  seurat_obj,
  score_pattern = "_logFC$",
  group.by = NULL,
  fun = c("mean", "median")
)
}
\arguments{
\item{seurat_obj}{A Seurat object with per-cell score columns in
\code{seurat_obj@meta.data}.}

\item{score_pattern}{Regular expression used to select score columns
(e.g. \code{"_logFC$"}).}

\item{group.by}{Optional metadata column name used to group cells prior
to summarization. If \code{NULL}, all cells are treated as one group.}

\item{fun}{Aggregation function to use for summarization, one of
\code{"mean"} or \code{"median"}.}
}
\value{
A data frame with one row per group (or a single row if
\code{group.by} is \code{NULL}), containing the group label, number
of cells, and the aggregated scores for each selected column.
}
\description{
This internal helper summarizes columns in \code{seurat_obj@meta.data}
whose names match \code{score_pattern} (e.g. all logFC columns ending
in \code{"M6_logFC"}). If \code{group.by} is provided, scores are
summarized within each group; otherwise, a single row summarizing all
cells is returned.
}
\note{
Currently unused by AlertSystemScore; kept for potential reuse.
}
\keyword{internal}
